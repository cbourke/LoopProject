

var min = 1;
var max = 100;
var x = Math.floor(Math.random() * (max + 1 - min)) + min;

var stringGuess = prompt("enter a value");
//values from a prompt are strings that need to be
//parsed to integers:
var g = parseInt(stringGuess);
var numGuesses = 1;

//TODO: finish the program...
